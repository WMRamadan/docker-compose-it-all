version: '3'

services:

  docker-fastapi:
   container_name: docker-fastapi
   image: ghcr.io/wmramadan/fastapi-boilerplate:latest
   ports:
     - 80:80
   volumes:
     - fastapi:/app/api/data

volumes:
  fastapi: